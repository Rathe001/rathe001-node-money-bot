export const config = {
  baseApi: 'https://paper-api.alpaca.markets',
  throttleDelay: 1000,
};

export const alpacaConfig = {
  keyId: 'key',
  secretKey: 'secret',
  paper: true,
};

export default { config, alpacaConfig };
